---
- name: "Cursor CLI | Install via native installer"
  ansible.builtin.shell:
    cmd: curl https://cursor.com/install -fsS | bash
    executable: /bin/bash
  args:
    creates: "{{ ansible_env.HOME }}/.local/bin/agent"
